{% extends 'base.html.twig' %}

{% block title %}Thematiques{% endblock %}

{% block body %}
    <h1>Thematiques</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ thematique.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ thematique.name }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ thematique.image }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ thematique.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('thematiques_index') }}">back to list</a>

    <a href="{{ path('thematiques_edit', {'id': thematique.id}) }}">edit</a>

    {{ include('thematiques/_delete_form.html.twig') }}
{% endblock %}
